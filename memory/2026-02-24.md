## Auto Summary (cron)
- Gmail/Proton連携方針を整理し、Gmail本体は触らず「転送受け口分離」で運用するのが安全という結論になった。
- read-only安全設計について、プロンプト制御だけでなくアカウント分離・承認ゲート・通信レイヤ制御（状態変更系の遮断）を重ねる方針を確認した。
- 口調設定として「にゃーにゃー強め」を永続化し、Slackの二重投稿は編集イベント拾いが原因候補と整理した。
- OpenClawユースケース収集用の新skill `openclaw-usecase-scout` を作成し、Web根拠URL必須ルールを追加した。
- 同skillを `.skill` でパッケージ化し、根拠URL付きユースケース提案の試運転を実施した。
- 毎朝8:00のユースケース通知cronを作成し、進捗確認cronは要望に合わせて「毎日18:00のみ」に調整した。
- 追加でLinuxローカル連携アイデア（Obsidian/API連携/ICS集約）を検討し、段階的に安全導入する方針を共有した。

## Manual Save (16:03 JST)
- `/restart` では要約の連続性が保証されない可能性を確認。
- ただし `/reset` は `session-memory` フック対象で、リセット前内容の保存に向いている点を確認。
- 「restart時hookできる？」への回答として、標準では `/new`・`/reset` 対応、`/restart` はカスタムhookが必要と整理。
- いま手動で本日の内容を memory ファイルへ保存実行。
